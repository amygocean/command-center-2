# Mentions tab visibility

- Replaced the Mentions pills with a fixed two-part segmented control.
- All and Hidden remain visible regardless of which view is selected.
- The active view uses a raised card, stronger label, count badge and teal underline.
- Added tab semantics and selected-state attributes for accessibility.
- Updated browser cache keys so the style change loads after deployment.
